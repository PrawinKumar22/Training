spring:
  application:
     name: driver 2
server:
  port: 4040
  
  
eureka:
   client:
     fetch-registry: true
     register-with-eureka: true
     service-url:
       defaultZone: http://localhost:8761/eureka
    
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"